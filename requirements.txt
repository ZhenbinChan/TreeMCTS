# Core dependencies
torch>=2.0.0
transformers>=4.30.0
datasets>=2.10.0
numpy>=1.21.0
tqdm>=4.60.0

# For vLLM acceleration (optional but recommended)
vllm>=0.2.0

# For DeepSpeed acceleration (optional)
deepspeed>=0.10.0

# Utilities
pydantic>=1.9.0
pyyaml>=6.0

# Development dependencies (optional)
pytest>=7.0.0
black>=22.0.0
isort>=5.0.0
